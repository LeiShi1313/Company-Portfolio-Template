import React from 'react';
import './Roadmap.css';
import Section from './Section';

const Roadmap = () => (
  <div className="main-page roadmap">
    <section>
      <Section/>
    </section>
  </div>
);

export default Roadmap;
